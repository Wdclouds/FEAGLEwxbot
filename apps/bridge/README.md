# FEAGLE Bridge

Node.js Bridge、Dashboard、Wechat4u/Android transport、OneBot v11 适配和 AstrBot 监督器。

该目录是 Docker 的独立构建上下文。服务器部署应从仓库根目录运行
`./feagle bridge setup`，不要在这里保存 `.env`、AstrBot 源码或运行数据。

本地测试：

```bash
npm ci
npm test
```
